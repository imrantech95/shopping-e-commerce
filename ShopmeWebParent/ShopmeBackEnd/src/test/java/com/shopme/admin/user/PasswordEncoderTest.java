package com.shopme.admin.user;

import static org.assertj.core.api.Assertions.assertThat;
import org.junit.jupiter.api.Test;

public class PasswordEncoderTest {

	/*
	 * @Test public void testEncoderPassword() { BCryptPasswordEncoder
	 * encoderPassword = new BCryptPasswordEncoder();
	 * 
	 * String rawPassword = "Imran1212"; String PasswordEncoded =
	 * encoderPassword.encode(rawPassword); System.out.println(PasswordEncoded);
	 * 
	 * boolean matches = encoderPassword.matches(rawPassword, PasswordEncoded);
	 * 
	 * assertThat(matches).isTrue(); }
	 */}
